package RESTAPI;
import java.sql.*;

public class SQLQueryBuilder {
/*    public static String sendRequest() {
        String requestURL;

        //TODO: ANTONIO add driver name to pass in Class.forName();
        Connection  conn = DriverManager.getConnection(requestURL, "root", "");

        String query = "SELECT * FROM CancerData"; //TODO: ANTONIO INSERT TABLE NAME

        Statement st = null;
        try {
            st = conn.createStatement();
        } catch (SQLException e) {
            e.printStackTrace();
        }
        ResultSet rs;
        try {
             rs = st.executeQuery(query);
        } catch (SQLException e) {
            e.printStackTrace();
        }

        while(rs.next()){

        }
    }*/
}
